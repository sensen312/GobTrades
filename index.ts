import 'react-native-gesture-handler'; // REQUIRED for react-native-draggable-flatlist
import { registerRootComponent } from 'expo';
import App from './App'; // Assuming App.tsx is at the root of the project

registerRootComponent(App);
